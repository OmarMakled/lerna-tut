{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "@commitlint/cli": "^13.2.1",
    "@commitlint/config-conventional": "^13.2.0",
    "husky": "^7.0.4",
    "lerna": "^4.0.0"
  }
}
